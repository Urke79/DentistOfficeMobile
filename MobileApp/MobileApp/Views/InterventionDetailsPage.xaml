<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Views.InterventionDetailsPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem 
                 Text="Obriši"
                 Order="Primary"
                 Priority="0" 
                 Command="{Binding DeleteInterventionCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>

        <TableView Intent="Form">
            <TableRoot>
                <TableSection>

                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="Datum" />
                            <DatePicker Date="{Binding SelectedIntervention.Date}" Format="d MMM yyyy" />
                        </StackLayout>
                    </ViewCell>

                    <EntryCell Label="Intervencija" Text="{Binding SelectedIntervention.InterventionDescription}" />

                    <EntryCell Label="Napomena" Text="{Binding SelectedIntervention.Note}" />

                    <EntryCell Label="Cena" Text="{Binding SelectedIntervention.Price}" Keyboard="Numeric" />
                   
                    <ViewCell>
                        <Button Text="Sačuvaj" Command="{Binding SaveInterventionCommand}" />
                    </ViewCell>

                </TableSection>
            </TableRoot>
        </TableView>

    </ContentPage.Content>
</ContentPage>